name = "flashycards"
compatibility_date = "2024-01-01"

# D1 Database binding for leaderboard
# After creating the database with `wrangler d1 create flashy-leaderboard`,
# replace <DATABASE_ID> with the actual database ID from the output.
[[d1_databases]]
binding = "DB"
database_name = "flashy-leaderboard"
database_id = "5d6290af-f06f-4d1f-be63-a11dd28fad5c"
